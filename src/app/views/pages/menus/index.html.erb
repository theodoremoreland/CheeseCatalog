<h1><%= @title || "Default Title" %></h1>

<ul>
  <% @menus.each do |menu| %>
    <li>
      <%= link_to menu.name, menu_view_path(menu.id) %>
    </li>
  <% end %>
</ul>

<%= link_to "Add Menu", menu_add_path %>